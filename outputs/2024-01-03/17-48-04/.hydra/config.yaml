hyperparameters:
  dataset_path: ~/datasets
  cuda: true
  DEVICE: torch.device("cuda" if cuda else "cpu")
  batch_size: 100
  x_dim: 784
  hidden_dim: 400
  latent_dim: 20
  learning_rate: 0.001
  epochs: 20
